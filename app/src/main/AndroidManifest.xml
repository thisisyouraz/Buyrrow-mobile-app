<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.mycompany.firebaseapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:label="Buyrrow"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".BiddersDetail"></activity>
        <activity
            android:name=".Bid_credential_AdType"
            android:theme="@style/AppTheme.NoActionBarwithStatusNotBlur" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".RetrieveMapsActivity"
            android:label="@string/title_activity_retrieve_maps" />
        <activity
            android:name=".BidAd_detail_admin"
            android:theme="@style/AppTheme.NoActionBarwithStatusNotBlur" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".Admin_approve_ad"
            android:theme="@style/AppTheme.NoActionBarwithStatusNotBlur" />
        <activity
            android:name=".AdminHome"
            android:theme="@style/AppTheme.NoActionBarwithStatusNotBlur" />
        <activity
            android:name=".BidDetail"
            android:theme="@style/AppTheme.NoActionBarwithStatusNotBlur" />
        <activity
            android:name=".CellRentDetail"
            android:theme="@style/AppTheme.NoActionBarwithStatusNotBlur" />
        <activity
            android:name=".About_us"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".My_ads"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Profile"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Create_Ad"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".signUp" />
        <activity
            android:name=".Home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".VerifyCodeActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>